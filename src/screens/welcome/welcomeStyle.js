import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textHeading: {
    color: "white",
    fontSize: 35,
    fontWeight: "bold",
    lineHeight: 40,
  },
  textDetail: { color: "lightblue", fontSize: 18, fontWeight: "400" },
});

export { styles };
